// PWAInstallPrompt — el banner de instalación está deshabilitado.
// El navegador mostrará su propio ícono de instalación en la barra de direcciones.
const PWAInstallPrompt = () => null;

export default PWAInstallPrompt;
